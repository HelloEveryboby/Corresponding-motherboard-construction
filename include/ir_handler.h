#ifndef IR_HANDLER_H
#define IR_HANDLER_H

/**
 * @brief Initializes the IR module and registers its command handlers.
 *
 * This function should be called once at startup. It registers the
 * handlers for IR-related commands with the main command processor.
 */
void IR_Init(void);


#endif // IR_HANDLER_H
